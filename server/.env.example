# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/felix

# JWT secret key for authentication (use a strong random string)
JWT_SECRET=your-secret-key-here

# Server port (optional, defaults to 4001)
PORT=4001

# Docker host (optional — auto-detects Windows/Linux socket if not set)
# DOCKER_HOST=http://localhost:2375
